//! `miz-convert` — pacman.conf -> miz.toml.
//!
//! Stub. Real conversion logic lands in a follow-up.

fn main() -> anyhow::Result<()> {
    eprintln!("miz-convert: not yet implemented");
    std::process::exit(1);
}
